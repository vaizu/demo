<div class="container">
  <p id="notice"><%= notice %></p>
  <div class="row mt-5">
    <h3>機能一覧</h3>
  </div>
  <div class="row">
    <div class="card mt-4 w-100">
      <div class="card-body">
        <div class="row">
          <h5 class="ml-3">ウィジェット → 一覧修正</h5>
        </div>

        <div class="card mt-2 w-100">
          <div class="card-body">
            <div class="row bg-primary text-light p-2">
              <div class="col-11">
                <i class="fas fa-comment mr-3"></i>並び順の変更の仕方
              </div>
              <div class="col-1">
                <button type="button" class="text-light bg-primary float-right" data-toggle="collapse" data-target="#demo2"><i class="fa fa-minus"></i></button>
              </div>
            </div>
            <div id="demo2" class="collapse show">
              <p class="mt-2", style="line-height:2rem">
                ウィジェットをクリックしたまま移動先まで動かします。［　並び順を変更する　］ボタンで決定します。
              </p>
            </div>
          </div>
        </div>

        <table>
          <thead>
            <tr>
              <th>Location</th>
              <th>Title</th>
              <th>Content</th>
              <th colspan="3"></th>
            </tr>
          </thead>

          <tbody>
            <% @widgets.each do |widget| %>
              <tr>
                <td><%= widget.location %></td>
                <td><%= widget.title %></td>
                <td><%= widget.content %></td>
                <td><%= link_to 'Show', widget %></td>
                <td><%= link_to 'Edit', edit_widget_path(widget) %></td>
                <td><%= link_to 'Destroy', widget, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>

        <br>

        <%= link_to 'New Widget', new_widget_path %>
      </div>
    </div>
  </div>
</div>
